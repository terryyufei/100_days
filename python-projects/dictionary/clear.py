def clear_string(input_string):
    """
    Removes leading and trailing whitespace from a string.
    
    Args:
        input_string (str): The string to be cleared.
    
    Returns:
        str: The cleared string.
    """
    return input_string.strip()
